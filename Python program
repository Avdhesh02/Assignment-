# Python program to classify a given number into negative, zero, or positive

# Taking input from the user
number = int(input("Enter a number: "))

# Classifying the number and printing the result
if number < 0:
    print("The number is negative.")
elif number == 0:
    print("The number is zero.")
else:
    print("The number is positive.")
